diff --git a/include/constants/species.h b/include/constants/species.h
--- a/include/constants/species.h
+++ b/include/constants/species.h
@@ -1606,7 +1606,10 @@ enum __attribute__((packed)) Species
 
     SPECIES_CUSTOM_START = SPECIES_GLIMMORA_MEGA,
     // Add any custom species between here and SPECIES_CUSTOM_END
+    SPECIES_CINDURSA,
+    SPECIES_PYROGRIZZ,
+    SPECIES_INFERURSA,
     SPECIES_CUSTOM_END,
 
     SPECIES_EGG = SPECIES_CUSTOM_END,
diff --git a/src/data/pokemon/species_info.h b/src/data/pokemon/species_info.h
--- a/src/data/pokemon/species_info.h
+++ b/src/data/pokemon/species_info.h
@@ -174,6 +174,187 @@ const struct SpeciesInfo gSpeciesInfo[] =
     #include "species_info/gen_8_families.h"
     #include "species_info/gen_9_families.h"
 
+    // PokeMan Journey original species.
+    // Battle graphics, icons and cries are placeholders until the final GBA assets are added.
+    // The temporary learnsets intentionally borrow the Cyndaquil family so the line is battle-ready
+    // as soon as the custom graphics and Battle Evolution trigger are wired in.
+    [SPECIES_CINDURSA] =
+    {
+        .baseHP        = 55,
+        .baseAttack    = 60,
+        .baseDefense   = 50,
+        .baseSpeed     = 55,
+        .baseSpAttack  = 65,
+        .baseSpDefense = 50,
+        .types = MON_TYPES(TYPE_FIRE),
+        .catchRate = 45,
+        .expYield = 65,
+        .evYield_SpAttack = 1,
+        .genderRatio = PERCENT_FEMALE(50),
+        .eggCycles = 20,
+        .friendship = STANDARD_FRIENDSHIP,
+        .growthRate = GROWTH_MEDIUM_SLOW,
+        .eggGroups = MON_EGG_GROUPS(EGG_GROUP_FIELD),
+        .abilities = { ABILITY_BLAZE, ABILITY_NONE, ABILITY_FLASH_FIRE },
+        .bodyColor = BODY_COLOR_BLACK,
+        .speciesName = _("Cindursa"),
+        .cryId = CRY_TEDDIURSA,
+        .natDexNum = NATIONAL_DEX_NONE,
+        .categoryName = _("Ember Cub"),
+        .height = 5,
+        .weight = 85,
+        .description = COMPOUND_STRING(
+            "Warm embers glow beneath its dark fur.\n"
+            "When excited, fire gathers around its\n"
+            "paws and brow. It is fiercely protective\n"
+            "of Trainers it learns to trust."),
+        .pokemonScale = 420,
+        .pokemonOffset = 8,
+        .trainerScale = 256,
+        .trainerOffset = 0,
+        .frontPic = gMonFrontPic_CircledQuestionMark,
+        .frontPicSize = MON_COORDS_SIZE(48, 48),
+        .frontPicYOffset = 8,
+        .frontAnimFrames = sAnims_None,
+        .frontAnimId = ANIM_V_SQUISH_AND_BOUNCE,
+        .backPic = gMonBackPic_CircledQuestionMark,
+        .backPicSize = MON_COORDS_SIZE(48, 48),
+        .backPicYOffset = 8,
+        .backAnimId = BACK_ANIM_CONCAVE_ARC_SMALL,
+        .palette = gMonPalette_CircledQuestionMark,
+        .shinyPalette = gMonShinyPalette_CircledQuestionMark,
+        .iconSprite = gMonIcon_QuestionMark,
+        .iconPalIndex = 0,
+        .pokemonJumpType = PKMN_JUMP_TYPE_SLOW,
+        FOOTPRINT(QuestionMark)
+        SHADOW(-1, 3, SHADOW_SIZE_S)
+        .levelUpLearnset = sCyndaquilLevelUpLearnset,
+        .teachableLearnset = sCyndaquilTeachableLearnset,
+    },
+
+    [SPECIES_PYROGRIZZ] =
+    {
+        .baseHP        = 75,
+        .baseAttack    = 85,
+        .baseDefense   = 70,
+        .baseSpeed     = 70,
+        .baseSpAttack  = 85,
+        .baseSpDefense = 70,
+        .types = MON_TYPES(TYPE_FIRE),
+        .catchRate = 45,
+        .expYield = 142,
+        .evYield_Attack = 1,
+        .evYield_SpAttack = 1,
+        .genderRatio = PERCENT_FEMALE(50),
+        .eggCycles = 20,
+        .friendship = STANDARD_FRIENDSHIP,
+        .growthRate = GROWTH_MEDIUM_SLOW,
+        .eggGroups = MON_EGG_GROUPS(EGG_GROUP_FIELD),
+        .abilities = { ABILITY_BLAZE, ABILITY_NONE, ABILITY_FLASH_FIRE },
+        .bodyColor = BODY_COLOR_BLACK,
+        .speciesName = _("Pyrogrizz"),
+        .cryId = CRY_URSARING,
+        .natDexNum = NATIONAL_DEX_NONE,
+        .categoryName = _("Blaze Bear"),
+        .height = 12,
+        .weight = 460,
+        .description = COMPOUND_STRING(
+            "Its growing strength feeds the flames\n"
+            "around its shoulders and forelegs.\n"
+            "It charges through danger without fear\n"
+            "when defending members of its group."),
+        .pokemonScale = 300,
+        .pokemonOffset = 3,
+        .trainerScale = 256,
+        .trainerOffset = 0,
+        .frontPic = gMonFrontPic_CircledQuestionMark,
+        .frontPicSize = MON_COORDS_SIZE(56, 56),
+        .frontPicYOffset = 4,
+        .frontAnimFrames = sAnims_None,
+        .frontAnimId = ANIM_V_STRETCH,
+        .backPic = gMonBackPic_CircledQuestionMark,
+        .backPicSize = MON_COORDS_SIZE(56, 56),
+        .backPicYOffset = 4,
+        .backAnimId = BACK_ANIM_H_SHAKE,
+        .palette = gMonPalette_CircledQuestionMark,
+        .shinyPalette = gMonShinyPalette_CircledQuestionMark,
+        .iconSprite = gMonIcon_QuestionMark,
+        .iconPalIndex = 0,
+        .pokemonJumpType = PKMN_JUMP_TYPE_NONE,
+        FOOTPRINT(QuestionMark)
+        SHADOW(-1, 8, SHADOW_SIZE_M)
+        .levelUpLearnset = sQuilavaLevelUpLearnset,
+        .teachableLearnset = sQuilavaTeachableLearnset,
+    },
+
+    [SPECIES_INFERURSA] =
+    {
+        .baseHP        = 95,
+        .baseAttack    = 115,
+        .baseDefense   = 90,
+        .baseSpeed     = 80,
+        .baseSpAttack  = 105,
+        .baseSpDefense = 85,
+        .types = MON_TYPES(TYPE_FIRE, TYPE_DARK),
+        .catchRate = 45,
+        .expYield = 240,
+        .evYield_Attack = 2,
+        .evYield_SpAttack = 1,
+        .genderRatio = PERCENT_FEMALE(50),
+        .eggCycles = 20,
+        .friendship = STANDARD_FRIENDSHIP,
+        .growthRate = GROWTH_MEDIUM_SLOW,
+        .eggGroups = MON_EGG_GROUPS(EGG_GROUP_FIELD),
+        .abilities = { ABILITY_BLAZE, ABILITY_NONE, ABILITY_FLASH_FIRE },
+        .bodyColor = BODY_COLOR_BLACK,
+        .speciesName = _("Inferursa"),
+        .cryId = CRY_URSARING,
+        .natDexNum = NATIONAL_DEX_NONE,
+        .categoryName = _("Inferno Bear"),
+        .height = 21,
+        .weight = 1450,
+        .description = COMPOUND_STRING(
+            "Its body burns like a walking furnace.\n"
+            "The hotter its fighting spirit becomes,\n"
+            "the brighter the markings across its fur\n"
+            "blaze with volcanic heat."),
+        .pokemonScale = 256,
+        .pokemonOffset = 0,
+        .trainerScale = 270,
+        .trainerOffset = 0,
+        .frontPic = gMonFrontPic_CircledQuestionMark,
+        .frontPicSize = MON_COORDS_SIZE(64, 64),
+        .frontPicYOffset = 0,
+        .frontAnimFrames = sAnims_None,
+        .frontAnimId = ANIM_GROW_VIBRATE,
+        .backPic = gMonBackPic_CircledQuestionMark,
+        .backPicSize = MON_COORDS_SIZE(64, 64),
+        .backPicYOffset = 0,
+        .backAnimId = BACK_ANIM_V_SHAKE,
+        .palette = gMonPalette_CircledQuestionMark,
+        .shinyPalette = gMonShinyPalette_CircledQuestionMark,
+        .iconSprite = gMonIcon_QuestionMark,
+        .iconPalIndex = 0,
+        .pokemonJumpType = PKMN_JUMP_TYPE_NONE,
+        FOOTPRINT(QuestionMark)
+        SHADOW(0, 12, SHADOW_SIZE_L)
+        .levelUpLearnset = sTyphlosionLevelUpLearnset,
+        .teachableLearnset = sTyphlosionTeachableLearnset,
+    },
+
     [SPECIES_EGG] =
     {
         .frontPic = gMonFrontPic_Egg,
